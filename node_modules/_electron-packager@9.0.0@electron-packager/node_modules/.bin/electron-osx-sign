#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_electron-osx-sign@0.4.7@electron-osx-sign/bin/electron-osx-sign.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_electron-osx-sign@0.4.7@electron-osx-sign/bin/electron-osx-sign.js" "$@"
  ret=$?
fi
exit $ret
